# Bar = require './Bar.coffee'
# Input = require './Input.coffee'
# css = require './ModelForm.less'

# class ModelForm extends component

# 	mapKeys: (key)->
# 		h Input,
# 			type: 
# 	render: (props)->
# 		return h Bar,
# 			vert: yes
# 			h 'form',
# 				className: css['model-form']
# 				props.keys.map @mapKeys